apply plugin: 'base'

task wrapper(type: Wrapper) {
  gradleVersion = '4.3.1'
}

task zip(type: Zip) {
	from('data') {
		into('Game/data')
	}
	
	from('Database') {
		into('Database')
	}
	
	destinationDir project.buildDir
}